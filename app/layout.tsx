import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Kabab House - Order by Phone 24/7",
  description:
    "Order delicious Middle Eastern food from Kabab House by phone anytime. AI-powered ordering, instant confirmation, ready in 20-30 minutes.",
  keywords: [
    "Kabab House",
    "Middle Eastern food",
    "Oak Creek",
    "phone ordering",
    "halal",
    "shawarma",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
